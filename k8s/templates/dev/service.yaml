apiVersion: v1
kind: Service
metadata:
  name: voting-dev
  namespace: dev
spec:
  selector:
    app: voting-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
